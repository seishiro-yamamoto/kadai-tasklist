<% if users.any? %>
  <ul>
    <% users.each do |user| %>
      <li>
        <img src="<%= gravatar_url(user, { size: 50 }) %>" alt="">
          <div>
            <%= user.name %>
          </div>
          <div>
            <p><%= link_to 'View profile', user_path(user) %></p>
          </div>
      </li>
    <% end %>
  </ul>
<% end %>